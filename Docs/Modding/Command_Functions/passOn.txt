passOn(Actor)
Used to pass an actor from one scene to the next scene, used with followUp() and getSpecific(PassedOn)

Scene 1:
passOn(Actor)
followUp(scene2)

Scene2:
WHO: Acto = getSpecific(PassedOn)
ACTION_UNIQUEID: whore_out_i
ACTION_NAME: Whore myself out
WHERE: interaction
WHEN: 0 - 24
MINUTES: 5 - 10
TIMEOUT_MINUTES: 0
ALSO_TIMEOUT: 
EFFECTS:
CONDITIONS:
MOVE_FIRST: true
SCENE_ALWAYS: whore_out_i
ANIMATION:
WHO: Actor = getTarget(); If Actor.isTemporary() && [WHERE == brothel || WHERE == hotel || WHERE == bar || WHERE == nightclub || WHERE == pub]     
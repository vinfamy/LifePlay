WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
    "Hello"
    Actor = generatePerson()
    Actor.show(2)
    Actor.dress()
    "Show up"
    "Yay"
    SceneEnd()
    
    // Actor = getSpecific(Dating)
    // Player.strip()
    // Sex(Player, Actor)
    // SceneEnd()
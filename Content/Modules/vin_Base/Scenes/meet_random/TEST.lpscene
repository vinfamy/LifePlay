WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

	SceneStart()
    setBackgroundCustom(sb_doctor_gynecologist)
    "Hello, try sneak?"
    0:: "Yes"
    1:: "No"
    
    If 0
        SneakGame()
        If Random(0, 100) < sneak
            "Sneak passed"
            SneakGame()
            If Random(0, 100) < sneak
                "Success again!"
            Else
                "Not so lucky this time"
            Endif
        Else
            "Sneak failed"
        Endif
    Endif
    
    SceneEnd()

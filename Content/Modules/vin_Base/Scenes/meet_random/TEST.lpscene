WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

	SceneStart()
    setBackgroundCustom(work)
    Actor = generatePersonTemporary()
    While Actor.isMale()
        Actor = generatePersonTemporary()
    Endwhile
    Sex(Actor, Player)
    SceneEnd()

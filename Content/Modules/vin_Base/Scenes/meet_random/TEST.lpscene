WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

	SceneStart()
    Player.strip()
    "Which scene to play?"
    0:: "Anal"
    1:: "Sleeping"
    
    Actor = generatePersonTemporary()
    While Actor.isSameGender(Player)
        Actor = generatePersonTemporary()
    Endwhile

    If 0
        Filter(Anal)
    Elseif 1
        Filter(Sleeping)
        TalkNonConsensual()
        Actor.closeEyes(true)
    Endif
    
    Sex(Player, Actor)
    SceneEnd()

WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    sceneStart()
    Actor = getPerson()
    While !Actor.isMale()
        Actor = getPerson()
    Endwhile
    exchangeContact(Actor)
    Actor.dress()
    Actor.show(2)
    "Sex"
    Sex(Player, Actor)
    Actor.show(2)
    "We put our clothes back"
    sceneEnd()
